Local HC32F460 add-ons live here.

These are driver-owned integrations that behave like plugins but are carried in
the base HC32F460 repository rather than as separate submodules.

Current contents:
- `spindle_select/`: local runtime spindle-selection addon derived from the
  STM32F4xx spindle selection plugin.
